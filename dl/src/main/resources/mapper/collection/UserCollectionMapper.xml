<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.donotlisten.collection.mapper.UserCollectionMapper">

    <resultMap id="collectionResultMap" type="com.donotlisten.collection.UserCollection">
        <id property="id" column="id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="existsByUserIdAndListeningId" resultType="boolean">
        select count(1) > 0
        from user_collections
        where user_id = #{userId}
          and listening_id = #{listeningId}
    </select>

    <select id="findByUserIdAndListeningId" resultMap="collectionResultMap">
        select id, created_at
        from user_collections
        where user_id = #{userId}
          and listening_id = #{listeningId}
    </select>

    <insert id="insert" parameterType="com.donotlisten.collection.UserCollection">
        insert into user_collections (id, user_id, listening_id, created_at)
        values (#{id}, #{user.id}, #{listening.id}, #{createdAt})
    </insert>
</mapper>
